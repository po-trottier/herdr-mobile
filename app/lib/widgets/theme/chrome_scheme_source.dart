/// Where a resolved chrome `ColorScheme` came from.
/// `chrome_scheme.dart`'s `ChromeScheme` produces this alongside a scheme.
library;

/// The fixed Herdr palette. Both platforms use this.
enum ChromeSchemeSource { fixed }
